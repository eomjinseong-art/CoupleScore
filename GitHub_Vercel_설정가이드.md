# GitHub × Vercel — 고정 주소로 운영하기

지금까지 배포할 때마다 `heart-sync-multi-master-final-v29` 처럼 주소가 계속 바뀌었습니다. 원인은 파일을 직접 올리는 방식(드래그 앤 드롭 또는 CLI)을 쓰면서 매번 **새 프로젝트**가 만들어졌기 때문입니다.

GitHub를 연결하면 이 문제가 근본적으로 해결됩니다. 저장소 하나가 프로젝트 하나에 묶이므로, 파일을 몇 번 고쳐도 주소는 **영구히 동일하게 유지**됩니다.

---

## 1단계 — GitHub 저장소 만들기

GitHub 계정이 없으시면 먼저 [github.com](https://github.com)에서 무료 가입하십시오.

로그인 후 우측 상단의 **+** 버튼을 누르고 **New repository**를 선택합니다. 아래와 같이 입력하십시오.

| 항목 | 입력값 |
|---|---|
| Repository name | `heart-sync` (앞으로 주소에 쓰일 이름이니 짧고 깔끔하게) |
| Description | 비워두어도 됩니다 |
| 공개 범위 | **Private** 권장 (Vercel 무료 플랜에서도 정상 배포됩니다) |
| Add a README file | 체크하지 마십시오 (첨부 파일에 이미 포함) |

**Create repository** 를 누르면 빈 저장소가 만들어집니다.

---

## 2단계 — 파일 올리기

첨부한 ZIP 파일을 컴퓨터에서 압축 해제하십시오. `heart-sync-deploy` 폴더 안에 파일 6개가 들어 있습니다.

방금 만든 저장소 화면에서 **uploading an existing file** 링크를 누른 뒤, 압축 해제한 폴더를 열어 **폴더가 아니라 안에 있는 파일들을 전부 선택**해서 드래그하십시오. 폴더째로 올리면 경로가 한 단계 깊어져 배포가 되지 않습니다.

> 올바른 구조: 저장소 최상단에 `index.html`, `app.html`, `ads.json` 이 바로 보이는 상태
> 잘못된 구조: 저장소 최상단에 `heart-sync-deploy` 폴더만 보이는 상태

파일이 목록에 나타나면 화면 하단의 **Commit changes** 버튼을 누릅니다.

참고로 `.gitignore` 파일은 점(.)으로 시작해 일부 운영체제에서 숨김 처리됩니다. 보이지 않으면 그 파일만 빼고 올리셔도 무방합니다.

---

## 3단계 — Vercel에 연결하기

[vercel.com](https://vercel.com)에 로그인하십시오. 기존 계정을 그대로 쓰시면 됩니다.

대시보드에서 **Add New → Project** 를 누릅니다. GitHub 계정 연동을 요청하면 **Install** 을 눌러 권한을 허용하십시오. 이때 접근 범위는 방금 만든 `heart-sync` 저장소만 선택해도 충분합니다.

저장소 목록에 `heart-sync` 가 나타나면 우측의 **Import** 를 누릅니다. 다음 화면에서 설정을 물어보는데, **아무것도 바꾸지 말고** 그대로 **Deploy** 를 누르십시오. 순수 HTML 사이트이므로 빌드 설정이 필요 없습니다.

1분 내에 배포가 완료되고 축하 화면이 나타납니다.

---

## 4단계 — 고정 주소 확인 (가장 중요)

배포 후 프로젝트 화면 상단에 **Domains** 항목이 보입니다. 여기에 표시되는 주소가 앞으로 계속 쓰실 **고정 주소**입니다.

```
heart-sync.vercel.app
```

이름 뒤에 버전 접미사가 붙지 않은 이 주소가 핵심입니다. 앞으로 파일을 몇 번 수정해도 이 주소는 바뀌지 않습니다.

만약 원하는 이름이 이미 사용 중이라면 **Settings → Domains** 에서 `heart-sync-app` 처럼 다른 이름을 추가할 수 있습니다. 여러 주소를 등록해도 모두 같은 사이트를 가리킵니다.

> 주의: 배포 이력에 보이는 `heart-sync-abc123xyz.vercel.app` 같은 긴 주소는 각 배포 시점의 스냅샷을 가리키는 임시 주소입니다. **절대 이 주소를 홍보에 쓰지 마십시오.** 홍보에는 반드시 위의 짧은 고정 주소만 사용하십시오.

---

## 5단계 — 앞으로 파일 수정하는 방법

이제 컴퓨터에 아무것도 설치하지 않고 브라우저에서 바로 수정할 수 있습니다.

GitHub 저장소에서 고치려는 파일(예: `app.html`)을 클릭하고, 우측 상단의 **연필 아이콘**을 누릅니다. 내용을 수정한 뒤 화면 아래 **Commit changes** 를 누르면 끝입니다.

Vercel이 변경을 자동으로 감지해 1분 내에 새 버전을 배포합니다. **주소는 그대로**이므로 틱톡 바이오나 랜딩 페이지 링크를 다시 고칠 필요가 전혀 없습니다.

파일 전체를 교체하려면 저장소에서 **Add file → Upload files** 로 같은 이름의 파일을 올리면 덮어써집니다.

---

## 6단계 — 랜딩 페이지 링크 갱신

고정 주소가 확정되면 저에게 알려주십시오. 랜딩 페이지의 '무료로 시작하기' 버튼을 새 고정 주소로 바꿔드리겠습니다. **이번 한 번만 바꾸면 앞으로는 영구히 손댈 필요가 없습니다.**

---

## 참고 — 기존 프로젝트 정리

Vercel에 그동안 만들어진 `v27`, `v28`, `v29` 같은 프로젝트가 여러 개 남아 있을 수 있습니다. 새 프로젝트가 정상 작동하는 것을 확인한 뒤, 각 프로젝트의 **Settings → 맨 아래 Delete Project** 에서 정리하시면 관리가 깔끔해집니다.

다만 기존 주소를 이미 어딘가에 공유하셨다면, 방문자가 오류 페이지를 보지 않도록 최소 몇 주간은 남겨두시는 편이 안전합니다.

---

## 요약

| 구분 | 지금까지 | 앞으로 |
|---|---|---|
| 주소 | 배포마다 변경 | `heart-sync.vercel.app` 영구 고정 |
| 수정 방법 | 파일 다시 업로드 → 새 주소 발급 | GitHub에서 편집 → 자동 배포 |
| 링크 관리 | 매번 틱톡·랜딩 페이지 수정 | 수정 불필요 |
| 이력 관리 | 없음 | 모든 변경 이력 보존, 되돌리기 가능 |
